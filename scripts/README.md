# Automation Scripts

- `build.sh` – Configures and builds the sample ASW project using CMake, storing logs in `logs/build.log`.
- `import_arxml.sh` – Invokes the SystemDesk CLI to import AUTOSAR configuration from `asw/config` into `asw/generated`.

Ensure the scripts are executed from within the Dev Container where the required dependencies are available.
